class Question:
    def __init__(self, prompt, answer):
        self.prompt = prompt
        self.answer = answer
        #Question.prompt Question.answer
        #(self, prompt, answer) = Question(prompt, answer)